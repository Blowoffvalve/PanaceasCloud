function triggerViewVideo(){
	var query_string = {};
  	var query = window.location.search.substring(1);
  	if(query != ""){
		var iframe_video = document.getElementById("frame");
		console.log(iframe_video);
		
  	}

  	
}

$(function (){
    triggerViewVideo();

});